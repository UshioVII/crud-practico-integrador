<!DOCTYPE html>
<html lang="es">

    <%- include('./partials/head',{
        title : 'Register'
    })  %> 


<body class="register">
    <%- include('partials/headerNavbar')  %> 

    <div class="container products-wrapper">
        <div class="row">
            <div class="col-12">
                <h2 class="products-title">Crea una cuenta, profe. (Aun no le implemente el Hash, porque eso en el siguente trabajo practico c:)</h2>
            </div>
        </div>
        <div class="col-12">
            <form action="" method="post">
                <div class="row product-detail">
                    <div class="col-12 col-md-6">
                        <label class="form-label" for="nombre">Nombre:</label>
                        <input class="form-input" type="text" id="nombre" name="firstname" value="<%= locals.old && old.firstname ? old.firstname : null %>">
                        <h3 class=""><%= locals.errors && errors.firstname ? errors.firstname.msg :null %></small>
                    </div>
                    <div class="col-12 col-md-6">
                        <label class="form-label" for="apellido">Apellido:</label>
                        <input class="form-input" min="0" step="0.1" max="10" type="text" id="apellido" name="lastname" value="<%= locals.old && old.lastname ? old.lastname : null %>">
                        <h3 class="col-12 col-md-6"><%= locals.errors && errors.lastname ? errors.lastname.msg :null %></h3>
                    </div>
                    <div class="col-12 col-md-6">
                        <label class="form-label" for="email">Email:</label>
                        <input class="form-input" type="email" id="email" name="email" value="<%= locals.old && old.email ? old.email : null %>">
                        <h3 class="form__text--error"><%= locals.errors && errors.email ? errors.email.msg :null %></h5>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="col-12 col-md-6">
                            <label class="form-label" for="password">Contraseña:</label>
                            <input class="form-input" type="password" id="password" name="password">
                            <h3 class="form__text--error"><%= locals.errors && errors.password ? errors.password.msg :null %></h3>
                        </div>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="form__box--column ">
                            <div class="form__box--flex">
                                <input class="form__checkbox" type="checkbox" id="terminos" name="terms">
                                <label class="form__label--" for="terminos">Acepto los <a href="/terms" target="_blank">términos y condiciones</a></label>
                            </div>
                            <div class="form__box--flex">
                                <h3 class="form__text--error"><%= locals.errors && errors.terms ? errors.terms.msg :null %></h3>
                            </div>
                        </div>
                    </div>
                    <div class="form-box">
                        <a href="" class="">Cancelar</a>
                        <button  class="form__button form__button--send" type="submit">Registrate</button>
                    </div>
                </div>
            </form>
        </div>

    </div>
</body>

</html>
